{
  "name": "get-countries",
  "version": "1.0.0",
  "description": "Small script that makes a GET request to the restcountries.eu api to get a full list of countries, extracts the name of each country from returned data and and writes name to a json file.",
  "main": "getCountries.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Stephen Brady",
  "license": "MIT"
}
